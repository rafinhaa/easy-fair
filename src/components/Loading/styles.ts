import { createStyleSheet } from "react-native-unistyles"

export const stylesheet = createStyleSheet((_theme) => ({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
}))
